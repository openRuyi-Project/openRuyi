From 9bdcad74a76ee021777d72b7418296a47e86cf24 Mon Sep 17 00:00:00 2001
From: cl2t <yihong.or@isrc.iscas.ac.cn>
Date: Wed, 9 Sep 2026 10:36:08 +0800
Subject: [PATCH] tests: fix format strings for current Go

Keep Go vet enabled by using constant format strings and formatting
integer message sequences as decimal numbers. Production code is unchanged.

Signed-off-by: cl2t <yihong.or@isrc.iscas.ac.cn>
---
 server/clustering_test.go      | 4 ++--
 server/server_clients_test.go  | 2 +-
 server/server_delivery_test.go | 2 +-
 server/server_sub_test.go      | 2 +-
 server/server_test.go          | 6 +++---
 stores/filestore_msg_test.go   | 4 ++--
 6 files changed, 10 insertions(+), 10 deletions(-)

diff --git a/server/clustering_test.go b/server/clustering_test.go
index 86da36a..fe1367d 100644
--- a/server/clustering_test.go
+++ b/server/clustering_test.go
@@ -6526,7 +6526,7 @@ func TestClusteringRestoreSnapshotCreateSnapshotAfterMsgsExpired(t *testing.T) {
 		t.Fatalf("Server is stuck starting")
 	case e := <-errCh:
 		if e != nil {
-			t.Fatalf(e.Error())
+			t.Fatalf("%s", e.Error())
 		}
 	}
 }
@@ -6811,7 +6811,7 @@ func TestClusteringRestoreSnapshotMsgsBailIfNoLeader(t *testing.T) {
 		t.Fatalf("Server should have exited after a certain number of failed attempts")
 	case e := <-errCh:
 		if e != nil {
-			t.Fatalf(e.Error())
+			t.Fatalf("%s", e.Error())
 		}
 	}
 
diff --git a/server/server_clients_test.go b/server/server_clients_test.go
index a376109..5fdeb43 100644
--- a/server/server_clients_test.go
+++ b/server/server_clients_test.go
@@ -174,7 +174,7 @@ func TestClientCrashAndReconnect(t *testing.T) {
 	wg.Wait()
 	select {
 	case e := <-errCh:
-		t.Fatalf(e.Error())
+		t.Fatalf("%s", e.Error())
 	default:
 	}
 
diff --git a/server/server_delivery_test.go b/server/server_delivery_test.go
index 09c2cbe..8e121f0 100644
--- a/server/server_delivery_test.go
+++ b/server/server_delivery_test.go
@@ -251,7 +251,7 @@ func TestDeliveryWithGapsInSequence(t *testing.T) {
 	}
 	select {
 	case e := <-errCh:
-		t.Fatalf(e.Error())
+		t.Fatalf("%s", e.Error())
 	default:
 	}
 }
diff --git a/server/server_sub_test.go b/server/server_sub_test.go
index a4251d6..4d59e3d 100644
--- a/server/server_sub_test.go
+++ b/server/server_sub_test.go
@@ -1145,7 +1145,7 @@ func TestSubAckInboxFromOlderStore(t *testing.T) {
 	// Now wait for more than AckWait to be sure that message is not redelivered
 	select {
 	case e := <-errCh:
-		t.Fatalf(e.Error())
+		t.Fatalf("%s", e.Error())
 	case <-time.After(1500 * time.Millisecond):
 		// ok...
 	}
diff --git a/server/server_test.go b/server/server_test.go
index 299f4a0..c5785eb 100644
--- a/server/server_test.go
+++ b/server/server_test.go
@@ -186,7 +186,7 @@ func init() {
 
 func stackFatalf(t tLogger, f string, args ...interface{}) {
 	msg := fmt.Sprintf(f, args...) + "\n" + stack()
-	t.Fatalf(msg)
+	t.Fatalf("%s", msg)
 	// For staticcheck SA0511...
 	panic("unreachable code")
 }
@@ -299,7 +299,7 @@ func waitFor(t tLogger, totalWait, waitInBetween time.Duration, f func() error)
 		time.Sleep(waitInBetween)
 	}
 	if err != nil {
-		stackFatalf(t, err.Error())
+		stackFatalf(t, "%s", err.Error())
 	}
 }
 
@@ -506,7 +506,7 @@ func runServerWithOpts(t *testing.T, sOpts *Options, nOpts *natsd.Options) *Stan
 	}
 	s, err := RunServerWithOpts(sOpts, nOpts)
 	if err != nil {
-		stackFatalf(t, err.Error())
+		stackFatalf(t, "%s", err.Error())
 	}
 	if testUseEncryption {
 		// Since the key was cleared when creating the crypto store,
diff --git a/stores/filestore_msg_test.go b/stores/filestore_msg_test.go
index 592cf68..a5c49b1 100644
--- a/stores/filestore_msg_test.go
+++ b/stores/filestore_msg_test.go
@@ -2029,7 +2029,7 @@ func TestFSReadBuffer(t *testing.T) {
 			t.Fatalf("Expected msg seq %v to be in cache, it was not", i)
 		}
 		if string(m.Data) != fmt.Sprintf("%v", i) {
-			t.Fatalf("Expected content to be %q, got %q", i, m.Data)
+			t.Fatalf("Expected content to be %d, got %q", i, m.Data)
 		}
 	}
 
@@ -2048,7 +2048,7 @@ func TestFSReadBuffer(t *testing.T) {
 			t.Fatalf("Expected msg seq %v to be in cache, it was not", i)
 		}
 		if string(m.Data) != fmt.Sprintf("%v", i) {
-			t.Fatalf("Expected content to be %q, got %q", i, m.Data)
+			t.Fatalf("Expected content to be %d, got %q", i, m.Data)
 		}
 	}
 

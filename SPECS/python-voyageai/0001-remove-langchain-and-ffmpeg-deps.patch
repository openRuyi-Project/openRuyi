--- voyageai-0.3.7/pyproject.toml.orig	2026-03-21 13:59:37
+++ voyageai-0.3.7/pyproject.toml	2026-03-21 14:02:13
@@ -18,8 +18,6 @@
 pillow = "*"
 pydantic = ">=1.10.8"
 tokenizers = ">=0.14.0"
-langchain-text-splitters = ">=0.3.8"
-ffmpeg-python = "*"
 
 [tool.poetry.group.test.dependencies]
 pytest = "^7.4.2"
@@ -42,4 +40,4 @@
 
 [build-system]
 requires = ["poetry-core"]
-build-backend = "poetry.core.masonry.api"
\ No newline at end of file
+build-backend = "poetry.core.masonry.api"
--- voyageai-0.3.7/voyageai/chunking.py.orig	2026-03-21 13:59:37
+++ voyageai-0.3.7/voyageai/chunking.py	2026-03-21 14:02:26
@@ -1,8 +1,6 @@
 from itertools import chain
 from typing import Callable, List
 
-from langchain_text_splitters import RecursiveCharacterTextSplitter
-
 DEFAULT_CHUNK_SIZE = 2048
 SEPARATORS = [
     "\n\n",
@@ -33,6 +31,13 @@
     """
     Simple wrapper for LangChain RecursiveCharacterTextSplitter.
     """
+    try:
+        from langchain_text_splitters import RecursiveCharacterTextSplitter
+    except ImportError as e:
+        raise ImportError(
+            "langchain-text-splitters is required for default_chunk_fn. "
+            "Install it with: pip install langchain-text-splitters"
+        ) from e
     splitter = RecursiveCharacterTextSplitter(
         separators=SEPARATORS,
         keep_separator="end",
